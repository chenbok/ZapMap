<resources>

    <style name="appBarStyle" parent="ThemeOverlay.AppCompat.ActionBar">
        <!-- color the title of the chosen tab -->
        <item name="android:textColorPrimary">@color/colorAccent</item>
        <!-- color the title of the Toolbar when parent is Theme.AppCompat.Light -->
        <!--<item name="android:textColorPrimaryInverse">@color/colorAccent</item>-->
        <!-- color the text of the secondary tab and action menu icons -->
        <item name="android:textColorSecondary">@color/colorAccent</item>
        <!-- color the overflow menu icon -->
        <!--<item name="actionMenuTextColor">@color/colorAccent</item>-->
        <!-- Color of the app bar -->
        <item name="android:background">@color/colorPrimary</item>
        <!--<item name="windowActionBar">false</item>-->
        <!--<item name="windowNoTitle">true</item>-->
    </style>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!--<style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:textColor">#008</item>
        <item name="android:windowTitleBackgroundStyle">@style/WindowTitleBackground</item>
    </style>

    <style name="WindowTitleBackground">
        <item name="android:background">#0000FF</item>
    </style>-->
</resources>
